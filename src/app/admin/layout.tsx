// Lead Enrichment Application - Admin Root Layout
// Base layout for all admin routes (both auth and dashboard)

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
