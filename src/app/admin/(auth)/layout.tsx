// Lead Enrichment Application - Auth Layout
// Simple layout for login page (no authentication check)

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
